# vim: syntax=ruby
# Bundler 0.8.1 Gemfile. I will remove bundler when I get the chance.
bundle_path 'gems'
disable_rubygems
disable_system_gems

gem 'big_money', '~> 1.1'
gem 'dm-core',   '~> 1.0'

only :development do
  gem 'jeweler'
  gem 'shoulda'
end
